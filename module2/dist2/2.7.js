"use strict";
{
    const getPropertyValue = (obj, key) => {
        return obj[key];
    };
    const user = {
        name: 'arju',
        age: 10,
        address: 'ctg'
    };
    const res = getPropertyValue(user, 'name');
    //
}
